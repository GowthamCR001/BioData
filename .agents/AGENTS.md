# Git Push Rule

Always commit and push changes to the GitHub repository automatically after completing a code modification task or feature implementation, unless the user explicitly states otherwise.

- Run `git add .`
- Run `git commit -m "<description of changes>"`
- Run `git push origin main`
